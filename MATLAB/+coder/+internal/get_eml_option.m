function val = get_eml_option(~)
% Stub for coder.internal.get_eml_option when MATLAB Coder is not installed.
% fmincon (R2026a) calls this via optim.coder.validate.checkProducts to check
% whether code-generation mode is active.  Returning false signals "normal
% interpreter execution" and lets the validation pass.
val = false;
end
